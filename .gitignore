# Локальные правила и данные Cursor IDE — не в репозитории
.cursor/

.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
# Архивы релиза (тяжёлые); сам каталог release/ и README в репозитории есть
release/*.tar.gz
.bundles/
.pytest_cache/
.ruff_cache/
logs/*.log
.env
deploy/.env
# Собранный образ (docker save) — большой бинарник, не коммитить
deploy/sdocs-mcp-ui-*.tar
deploy/sdocs-mcp-ui-*.tar.gz
deploy/stack-mcp-ui-*.tar
deploy/stack-mcp-ui-*.tar.gz
# URL прокси с учётными данными для docker build --secret (не коммитить)
build-proxy.url
**/build-proxy.url
config.yaml
# Локальные артефакты Playwright, если поднимаете mcp-playwright отдельно
playwright-browsers/*
