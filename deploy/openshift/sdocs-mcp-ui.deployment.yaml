# Пример Deployment/Service для OpenShift / Kubernetes.
#
# Перед применением: укажите image из вашего реестра; пути volume — внутри Pod.
# Secret с токенами — отдельно, например:
#   oc create secret generic sdocs-mcp-ui-env \
#     --from-literal=SDOCS_MCP_UI_TOKEN=... \
#     --from-literal=SDOCS_MCP_METRICS_TOKEN=...
# и раскомментируйте envFrom.
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: sdocs-mcp-config
  labels:
    app: sdocs-mcp-ui
data:
  config.yaml: |
    modules:
      postgres:
        enabled: false
      redis:
        enabled: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sdocs-mcp-ui
  labels:
    app: sdocs-mcp-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sdocs-mcp-ui
  template:
    metadata:
      labels:
        app: sdocs-mcp-ui
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: sdocs-mcp-ui
          image: sdocs-mcp-ui:0.6.6
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8888
              protocol: TCP
          env:
            - name: SDOCS_MCP_CONFIG
              value: /config/config.yaml
            - name: SDOCS_MCP_EMBED_MCP
              value: "true"
            - name: SDOCS_MCP_UI_WORKERS
              value: "1"
            - name: SDOCS_MCP_UI_AUDIT_LOG_PATH
              value: /app/data/logs/ui-audit.log
            # Несколько реплик без sticky / UI_WORKERS>1: раскомментируйте
            # - name: SDOCS_MCP_STATELESS_HTTP
            #   value: "true"
          # envFrom:
          #   - secretRef:
          #       name: sdocs-mcp-ui-env
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
            - name: data
              mountPath: /app/data
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 512Mi
      volumes:
        - name: config
          configMap:
            name: sdocs-mcp-config
        - name: data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: sdocs-mcp-ui
  labels:
    app: sdocs-mcp-ui
spec:
  selector:
    app: sdocs-mcp-ui
  ports:
    - name: http
      port: 8888
      targetPort: http
